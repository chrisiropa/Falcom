using System;
using System.Collections.Generic;
using System.Text;

namespace Falcom
{
    public enum ProcessState
    {
        Idle,
        ScrapStored,
        FineDosingComplete,
        MeltingProcessStarted
    }
}
